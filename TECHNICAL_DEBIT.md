- **[CMS] Agendamento de publicação e operações CRUD agendadas**: A feature de agendamento foi removida do escopo da Feature 021 por ser mais complexa do que o estimado. O agendamento deve ser tratado de forma holística — não apenas para publicação de páginas, mas para qualquer operação CRUD (arquivamento, ativação, rejeição com data futura). Requer: (1) modelo genérico de tarefas agendadas por entidade/ação; (2) worker Celery dedicado (`celery_cms_worker`) com queue `cms_events`; (3) ETA preciso com `apply_async(eta=datetime)`; (4) armazenamento de `celery_task_id` para cancelamento explícito; (5) estado `scheduled` no state machine de páginas; (6) idempotência de tasks (verificar estado antes de executar); (7) observabilidade de falhas de agendamento (`cms.scheduled_publish_failed`). Impacto: adicionar campo `scheduled_at` + `celery_task_id` em `thedevkitchen.cms.page`, novo estado `scheduled` na state machine (5 estados), e nova task em `celery_worker/tasks/`. Depende de: RabbitMQ ativo, `celery_cms_worker` configurado em `docker-compose.yml`.
- criar teste e2e para validar se o menu imobiliario esta ativo, validar se o menu técnico esta aparecendo
- usar uuid para identificar imóveis ao invés de id sequencial (rotas ainda usam `<int:property_id>`)
- Configurar cors nos endpoints, mas a configuração deve ser dinamica via interface web do backoffice menu technical (atualmente usa `cors='*'` estático em todos os endpoints)
- substituir consultas ao banco de dados por cache no redis para o processo de login (JWT já implementado; falta integração Redis no fluxo de sessão em `performance_service.py`)
- incluir a consulta do session_id e do JWT no redis, para ganho de performance (Redis configurado no docker mas não utilizado em produção para lookup de sessão/JWT)
- validar se faz sentido manter o `id` sequencial na tabela `thedevkitchen_api_session` — o `session_id` já é único e o id sequencial pode ser limitador para grandes volumes (índices já adicionados)
- arquivos que tem o skip como tracking_disable, podem ser problema de segurança, validar se é necessário manter desta forma?
- melhorar a documentação técnica do sistema de RBAC e dos perfis de usuários
- **[RBAC] Autorização por cargo é global, não por empresa**: A autorização de ações (o que cada cargo pode fazer) usa `user.has_group('quicksol_estate.group_real_estate_manager')` do Odoo, que é **global** — se o usuário está no grupo, está em todas as empresas ao mesmo tempo (`res_groups_users_rel` não tem `company_id`). A tabela `thedevkitchen_estate_profile` já armazena `(partner_id, company_id, profile_type_id)` — ou seja, um usuário pode ser `manager` na Empresa A e `agent` na Empresa B — mas essa informação **não é usada na autorização de ações**, apenas na busca de dados do perfil (ex: `_get_user_profile()` em `invite_controller.py` e `GET /api/v1/me`). O correto seria: ao verificar se um usuário pode executar uma ação na Empresa X, buscar o perfil via `thedevkitchen_estate_profile.search([('partner_id','=',user.partner_id.id),('company_id','=',X)])` e usar o `profile_type_id.code` para decidir. Impacto atual: um usuário convidado como `agent` na Empresa A pode agir como `agent` também na Empresa B se tiver acesso a ela, mesmo sem ter perfil lá. Corrigir exige: (1) helper `get_user_role_for_company(user, company_id)` usando `thedevkitchen_estate_profile`; (2) substituir `has_group()` nas matrizes de autorização por esse helper; (3) atualizar `PROFILE_CREATION_MATRIX` em `profile_api.py` e `invite_controller.py`; (4) atualizar resposta do `GET /api/v1/me` para incluir `role` por empresa dentro do array `companies[]`.
- Criar atividade para tudo o que for alterado via aplicação qualquer CRUD seja logado, para termos auditoria das atividades.
- **[Qualidade] `profile_api.py` abaixo do gate de Pylint ≥ 8.0/10 (Feature 027)**: `create_profile` (complexidade ciclomática 17), `list_profiles` (19), `update_profile` (19) e `delete_profile` (16) excedem o limite de complexidade do flake8 (`C901`, máx. 15), deixando o pylint do arquivo em 7.53/10 — dívida pré-existente antes da Feature 027 (baseline 7.47/10; a Feature 027 não piorou, e chegou a melhorar levemente ao remover a dependência de `request.env` em `_serialize_profile`). Aceito como exceção documentada durante a Feature 027 (unificação agent/profile) para não expandir o escopo para refatoração de métodos não relacionados. Corrigir exige quebrar esses 4 métodos em funções auxiliares menores (mesmo padrão já usado para `_serialize_profile`/`_deactivate_profile_cascade`), sem mudar comportamento — precisa de cobertura de teste antes de refatorar.
- **[Teste] Asserção tautológica em `test_agent_fields_write_through_to_linked_agent` (Feature 027, Task 4)**: `18.0/extra-addons/quicksol_estate/tests/integration/test_update_profile_agent_fields.py` contém `self.assertEqual(agent.creci_normalized[:10], "SP" if False else agent.creci_normalized[:10])` — como a condição do ternário é sempre `False`, o lado direito sempre resolve para o próprio `agent.creci_normalized[:10]`, tornando a asserção um `assertEqual(X, X)` que nunca falha, independente do valor real de `creci`/`creci_normalized`. O teste também não verifica `bank_account` nem `pix_key`, apesar do nome do teste prometer cobertura de "write-through" completa. O bug é originário do próprio plano (`plan-idea.md`, código literal da Task 4), não introduzido pelo implementador, que sinalizou o problema em vez de escondê-lo. A funcionalidade em si já está coberta corretamente por outras asserções no mesmo arquivo (bank_name/bank_account_type/bank_branch) e pelo teste de conflito de CRECI (`test_duplicate_creci_same_company_raises_validation_error`). Corrigir exige trocar a asserção por `assertTrue(agent.creci_normalized.startswith("SP"))` (ou equivalente) e adicionar `assertEqual(agent.bank_account, "9999-0")` / `assertEqual(agent.pix_key, "pix027@example.com")`.
- **[Testes/Ambiente] `scripts/validate_coverage.sh` falha em 4 scripts E2E de Feature 009 por dados de teste sujos, não por regressão de código (verificado durante o quality gate da Feature 027)**: `test_us9_s2_forgot_password.sh`, `test_us9_s2_set_password.sh`, `test_us9_s4_authorization_matrix.sh` e `test_us9_s6_resend_invite.sh` falham com `409 Email already exists` em e-mails fixos de fixture (ex.: `rbac_bootstrap_manager@example.com`) e com erros de FK (`real_estate_agent_profile_id_fkey`/`real_estate_agent_user_id_fkey`/`real_estate_lead_agent_id_fkey`) durante a limpeza (`DELETE`) de dados de execuções anteriores — `integration_tests/cleanup_test_data.sh --all` não dá conta de todas as cadeias de FK (ex.: `real_estate_agent` referenciado por `real_estate_lead`, perfis antigos com `agent_id` órfão). Confirmado que não é regressão do código: `profile_api.py`/`invite_controller.py` não foram alterados nesses fluxos, os 330 testes unitários de `quicksol_estate` e os 86 de `thedevkitchen_apigateway` passam 100%, e os erros re-aparecem de forma idêntica mesmo após rodar a limpeza manualmente. Corrigir exige: (1) expandir `cleanup_test_data.sh` para deletar em ordem de dependência (leads → agents → profiles/users), e/ou (2) os scripts de teste Feature 009 pararem de assumir e-mails de fixture fixos/reutilizáveis entre execuções.
- **[Testes] `thedevkitchen_user_onboarding`'s generic unit-test runner (`_test_tools/run_generic_unit_tests.py`) quebra em `test_email_link_settings.py` (pré-existente, Feature 009, commit `6207cb0`)**: `AttributeError: 'TestEmailLinkSettings' object has no attribute 'test_tags'` — a classe é um teste no estilo Odoo `TransactionCase` (precisa de registry/DB), mas está sendo descoberta pelo runner genérico baseado em `unittest.loader` puro (pensado só para `unittest.TestCase` sem Odoo), que não inicializa `test_tags`. Descoberto durante o quality gate da Feature 027 (`scripts/validate_coverage.sh` reporta "1 unit test module failed"); não relacionado às mudanças desta feature em `invite_controller.py` (só a HATEOAS link foi alterada). Corrigir exige mover `test_email_link_settings.py` para rodar via `odoo --test-enable` (como os demais testes `TransactionCase` do módulo) em vez do runner genérico, ou adaptar o runner genérico para pular classes que herdam de `TransactionCase`.